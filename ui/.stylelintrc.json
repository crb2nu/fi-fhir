{
  "comment": "Token-drift guardrail (UX professional redesign §5.3). Forbids raw hex in component <style> blocks so color flows through tokens.css. BLOCKING as of Slice 1 (all 55 offenders migrated to tokens). tokens.css/base.css are intentionally NOT linted (they are the source of truth for raw values).",
  "overrides": [
    {
      "files": ["**/*.svelte"],
      "customSyntax": "postcss-html"
    }
  ],
  "rules": {
    "color-no-hex": [
      true,
      {
        "message": "Raw hex forbidden in component styles — use a token from tokens.css (var(--color-*)). See .loom/21-ux-professional-redesign.md §5.3."
      }
    ]
  }
}
