apiVersion: v1
kind: Service
metadata:
  name: {{ include "fi-fhir.fullname" . }}
  labels:
    {{- include "fi-fhir.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "fi-fhir.selectorLabels" . | nindent 4 }}
