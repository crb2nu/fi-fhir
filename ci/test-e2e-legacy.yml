# Lane S6-C — legacy e2e tree repair (.loom/34, Lane S6-C).
#
# Pre-registered by the Sprint 6 merge-surface MR so the include line in
# .gitlab-ci.yml and this file exist before the lane's MR opens. Lane S6-C
# replaces this comment with the blocking `test:e2e-legacy` job: the binary
# built and exec'd against declared service containers, exactly as
# ci/s5b-chaos-dr.yml does, running ./test/e2e/... with
# FI_FHIR_E2E_REQUIRED_SERVICES declared so an unreachable dependency fails
# instead of skipping.
#
# A comment-only include is valid GitLab CI and adds no job, which is what
# scripts/ci-job-inventory.sh --check verifies until the lane fills it.
